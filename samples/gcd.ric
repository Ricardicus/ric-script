# A script showing how the GCD of two numbers can be determined

@ calcGCD(a, b) {
    [ b == 0 ] { 
      return a
    } ~ { 
      return calcGCD(b , (a % b) )
    }
}

[ argN < 3 ] {
	print('usage: ' + arg0 + ' number1 number2')
	exit(1)
}

# Numbers to calculate GCD of
x = arg1
y = arg2

print("GCD of " + x + " and " + y + ", is: " +  calcGCD(x, y) )
