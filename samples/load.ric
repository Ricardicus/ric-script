# This script is developed to be used in the test suite
# run under ric-script/tests, so that pathing is OK
load("../samples/foobar.ric")
foo()
bar()
foobarfoobar()
foobar("yes, loading works!")
fbc = foobarclass()
fbc::foo()
fbc::bar()

load("../samples/load_test/definitions.ric")

print(sum(getLinesAsInts("../samples/load_test/numbers.txt")))
