@ getLines(file) {
  ? [ isFile(file) ] {
    -> fileReadLines(fileOpen(file))
  }
  -> []
}

@ getLinesAsInts(file) {
  ? [ isFile(file) ] {
    -> [ ( fileReadLines(fileOpen(file)) ... line ) { parseInt(line) } ]
  } 
  -> []
}
