# A function that will be recursive
@ recursive(a) {
  [ a == 0 ] {
    "I am done with the recursion now!"
  } ~ {
    "-- a: " + a
    recursive( a - 1 )
    "++ a: " + a
  }
}

recursive(100)

"Yey! The interpreter is doing recursion all fine!"